# twoIndices by Neeza Thandi
#
# This function take an array of integers and a target value, returning the indices of two numbers that add
# up to the target (or an empty array if such indices do not exist).


def two_indices(integers_in, target):
    # Input
    #    integersIn: an array of unique integers
    #    target: a target integer
    # Output
    #    an array of two integers, with the smaller index first and the larger second.
    #    Return an empty array if no solution found.
    array_out = []

    return array_out
